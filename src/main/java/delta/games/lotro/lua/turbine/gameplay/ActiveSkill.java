package delta.games.lotro.lua.turbine.gameplay;

/**
 * ActiveSkill library for lua scripts.
 * @author MaxThlon
 */
public class ActiveSkill {
  public ActiveSkill() {}
/*
  @Override
  public LuaValue call(LuaState state) throws LuaError, UnwindThrowable {
    LuaTable library = ValueFactory.tableOf();
    library.rawset("GetBaseCooldown", new GetBaseCooldown());
    return library;
  }

  static class GetBaseCooldown extends ZeroArgFunction {
  
    @Override
    public LuaValue call(LuaState state) throws LuaError {
      return Constants.NIL;
    }
  }*/
}

